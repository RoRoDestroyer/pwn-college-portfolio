[Intro to Commands]

Module: [Hello Hackers / Linux Luminarium]

Date: [8/27/2026]

[Objective]:

Write your first command, specifically invoke the "hello" command (custom pwn.college script non native Linux command).

[Key Concept]:

Commands typed into the terminal will be executed upon pressing enter. Commands in Linux are case sensitive.

[Solution]:

Simply invoke the "hello" command in the Linux terminal.

[# Relevant command(s) or code]:

Albeit not used for the challenge "whoami" command prints the name of the current logged in user in the terminal. "hello" command is relevant as it is used to complete this challenge; this command is non native to Linux and is used strictly for this challenge.

[What I Learned]:

The whoami command is one of many commands on Linux; in specific this command prints the current logged in users name to the terminal. Linux commands are case sensitive and upon the completion of a command the terminal is ready for the next command. 

Status: ✓ Completed on pwn.college

All work was performed within the authorized pwn.college educational environment. Challenge flags, credentials, and other restricted values are intentionally omitted.
